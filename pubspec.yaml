name: quran_tracker
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.1
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  uuid: ^4.5.1
  provider: ^6.1.5+1
  audioplayers: ^6.5.1
  flutter_launcher_icons: ^0.14.4
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# ==================== أيقونة التطبيق ====================
flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/quran.png"
# ========================================================

flutter:
  uses-material-design: true

  assets:
    - assets/img/

  # لإضافة خطوط مخصصة إذا أردت
  # fonts:
  #   - family: YourFontFamily
  #     fonts:
  #       - asset: fonts/YourFont.ttf
